# 日本麻将快速算分网页需求文档

## 1. 背景与目标

本项目需要提供一个面向手机使用的日本麻将快速算分网页。用户打开网页后，可以通过点击麻将牌图片和少量开关，快速录入当前手牌、副露、和牌与场况信息；系统基于 Python `mahjong` 库计算番数、符数、役种、点数和符明细，并以清晰方式展示给用户。

核心目标是“快速输入优先”：尽量减少键盘输入，所有常用信息都应能通过点选、切换、加减按钮完成。

## 2. 用户场景

1. 用户在牌局中需要快速计算一手牌的得点。
2. 用户用手机打开网页。
3. 用户依次点选当前手牌、副露、和牌牌、宝牌和场况。
4. 用户点击“计算”后，网页展示番数、符数、役种、点数、亲/子支付方式和符明细。
5. 用户可一键清空或回退误选，继续录入下一手牌。

## 3. 资源约定

现有图片位于 `img/` 目录，文件命名如下：

- 万子：`1m.png` 至 `9m.png`，红五为 `0m.png`
- 筒子：`1p.png` 至 `9p.png`，红五为 `0p.png`
- 索子：`1s.png` 至 `9s.png`，红五为 `0s.png`
- 字牌：`1z.png` 至 `7z.png`

牌面编码沿用 `mahjong` 库常用表示：

- `m` 表示万子
- `p` 表示筒子
- `s` 表示索子
- `z` 表示字牌
- `0m/0p/0s` 表示红五

## 4. 范围

### 4.1 必须支持

- 手机端网页访问和操作。
- 使用图片快速点选手牌。
- 当前手牌输入。
- 副露输入，支持区分：
  - 吃
  - 碰
  - 明杠
  - 暗杠
  - 加杠
- 和牌牌选择。
- 场况输入：
  - 和牌方式：自摸/荣和
  - 是否庄家
  - 场风
  - 自风
  - 本场数/连庄次数
  - 立直
  - 一发
  - 海底/河底
  - 岭上
  - 抢杠
  - 天和/地和
  - 宝牌指示牌
  - 里宝牌指示牌
  - 是否启用红宝牌
- 调用 Python `mahjong` 库计算：
  - 番数
  - 符数
  - 役种
  - 点数
  - 符明细
  - 错误信息
- 支持一键清空、撤销上一步。

### 4.2 暂不要求

- 用户登录。
- 历史记录同步。
- 多语言。
- 牌谱导入。
- 自动图片识别。
- 在线对战或完整牌局管理。

## 5. 页面与交互需求

### 5.1 页面整体布局

手机端首屏应优先展示当前录入状态和牌面选择区。推荐结构：

1. 顶部状态区：显示当前已选手牌数量、副露数量、和牌牌、是否可计算。
2. 当前手牌区：展示已选牌，支持点击删除单张。
3. 副露区：展示已添加副露，支持编辑和删除。
4. 快速选牌区：按“万/筒/索/字”分组展示牌图。
5. 场况区：使用折叠面板或底部抽屉，避免挤占主要选牌空间。
6. 计算结果区：计算后展示主要结果。

### 5.2 当前手牌输入

- 用户点击牌图后，将该牌加入当前录入目标。
- 默认录入目标为“当前手牌”。
- 当前手牌应按花色和数字自动排序展示。
- 每张实体牌最多只能出现 4 次。
- 红五按红宝牌实体处理：
  - `0m` 与普通 `5m` 总数量合计最多 4 张。
  - `0p` 与普通 `5p` 总数量合计最多 4 张。
  - `0s` 与普通 `5s` 总数量合计最多 4 张。
- 点击已选手牌中的牌可删除该牌。
- 提供“撤销”按钮撤回最近一次添加或删除。

### 5.3 和牌牌输入

- 用户必须选择一张和牌牌。
- 和牌牌可以通过以下方式快速指定：
  - 点击当前手牌中的某张牌后选择“设为和牌牌”。
  - 或在选择牌时开启“下一张设为和牌牌”模式。
- 和牌牌必须包含在传给 `mahjong` 库的 14 张牌内。
- 和牌牌在手牌展示中需要有明显标识。

### 5.4 副露输入

副露录入应尽量少步骤完成。

#### 5.4.1 副露输入模式

提供“副露模式”入口，用户选择副露类型后点选牌：

- 吃：选择三张连续同花色数牌。
- 碰：选择三张相同牌。
- 明杠：选择四张相同牌，`opened=True`。
- 暗杠：选择四张相同牌，`opened=False`。
- 加杠：选择四张相同牌，按 `Meld.SHOUMINKAN` 处理。

#### 5.4.2 快速生成

- 当用户选择“碰/明杠/暗杠/加杠”模式后，只需点击一张牌，系统自动生成对应 3 张或 4 张副露。
- 当用户选择“吃”模式后：
  - 用户可以点选三张牌。
  - 若三张牌不合法，应立即提示并阻止添加。
- 添加副露后，副露区以横向牌组展示。
- 副露牌组旁显示类型标签：吃、碰、明杠、暗杠、加杠。
- 暗杠需要与明杠有视觉区分，例如暗杠牌背或明显“暗”标签。

### 5.5 场况输入

场况应使用开关、分段按钮、步进器和牌图选择，避免文本输入。

#### 5.5.1 基础场况

- 和牌方式：自摸/荣和。
- 是否庄家：是/否。
- 场风：东/南/西/北。
- 自风：东/南/西/北。
- 本场数：数字步进器，默认 0。

说明：用户原始需求中提到“连庄次数”，在日本麻将点数支付中通常需要体现在本场数上，产品文案可显示为“本场数/连庄次数”。

#### 5.5.2 役相关状态

使用开关录入：

- 立直
- 双立直
- 一发
- 门前清自摸由系统根据副露和自摸状态判断，不需要用户单独输入。
- 海底摸月
- 河底捞鱼
- 岭上开花
- 抢杠
- 天和
- 地和
- 人和

交互限制：

- 自摸时可选海底，荣和时可选河底。
- 天和/地和与立直、一发、海底、河底、岭上、抢杠互斥。
- 有副露时不能选择立直、双立直、一发。

#### 5.5.3 宝牌输入

- 宝牌指示牌使用与手牌相同的牌图选择。
- 支持添加多张宝牌指示牌。
- 里宝牌指示牌使用相同方式录入。
- 里宝牌只在立直或双立直时参与计算。
- 红宝牌开关默认开启或按配置决定。

### 5.6 结果展示

计算成功后展示：

- 主要点数：
  - 荣和：显示放铳者支付点数。
  - 自摸：显示亲/子各自支付点数，若用户为亲则显示每家支付点数。
- 番数。
- 符数。
- 役种列表。
- 宝牌/里宝牌数量如果 `mahjong` 库返回在役种中，应直接展示。
- 符明细列表。
- 原始输入摘要，方便用户核对。

计算失败时展示：

- 错误原因，例如无役、牌数不合法、和牌形不成立、输入组合非法。
- 引导用户检查手牌数量、和牌牌、副露和场况。

## 6. 数据模型

### 6.1 前端状态

```json
{
  "closedTiles": ["2m", "2m", "4m", "4m", "4m", "3p", "3p", "3p", "5p", "6p", "7p", "4s", "4s", "4s"],
  "winTile": "4s",
  "melds": [
    {
      "type": "pon",
      "tiles": ["4m", "4m", "4m"],
      "opened": true
    },
    {
      "type": "kan",
      "tiles": ["1z", "1z", "1z", "1z"],
      "opened": false
    }
  ],
  "round": {
    "isTsumo": false,
    "isDealer": false,
    "playerWind": "east",
    "roundWind": "east",
    "honba": 0
  },
  "flags": {
    "isRiichi": false,
    "isDoubleRiichi": false,
    "isIppatsu": false,
    "isHaitei": false,
    "isHoutei": false,
    "isRinshan": false,
    "isChankan": false,
    "isTenhou": false,
    "isChiihou": false,
    "isRenhou": false
  },
  "doraIndicators": ["1m"],
  "uraDoraIndicators": [],
  "options": {
    "hasOpenTanyao": true,
    "hasAkaDora": true
  }
}
```

### 6.2 后端请求

接口：`POST /api/calculate`

请求体使用前端状态的 JSON 结构。

后端负责：

- 校验牌数量和副露合法性。
- 将牌编码转换为 `mahjong` 库可用的 136 牌格式。
- 构建 `Meld`、`HandConfig`、`OptionalRules`。
- 调用 `HandCalculator.estimate_hand_value()`。
- 返回标准化结果。

### 6.3 后端响应

成功响应：

```json
{
  "ok": true,
  "han": 4,
  "fu": 40,
  "cost": {
    "main": 4000,
    "additional": 2000
  },
  "yaku": ["Menzen Tsumo", "Tanyao", "San Ankou"],
  "fuDetails": [
    { "fu": 20, "reason": "base" },
    { "fu": 4, "reason": "closed_pon" },
    { "fu": 2, "reason": "tsumo" }
  ],
  "error": null
}
```

失败响应：

```json
{
  "ok": false,
  "han": null,
  "fu": null,
  "cost": null,
  "yaku": [],
  "fuDetails": [],
  "error": "手牌数量不合法：当前组合应合计为 14 张牌"
}
```

## 7. `mahjong` 库集成要求

后端使用以下核心 API：

- `HandCalculator.estimate_hand_value()`
- `TilesConverter.string_to_136_array()`
- `HandConfig`
- `OptionalRules`
- `Meld`

副露类型映射：

- 吃：`Meld.CHI`
- 碰：`Meld.PON`
- 明杠：`Meld.KAN`，`opened=True`
- 暗杠：`Meld.KAN`，`opened=False`
- 加杠：`Meld.SHOUMINKAN`

风牌映射：

- 东：`EAST`
- 南：`SOUTH`
- 西：`WEST`
- 北：`NORTH`

配置映射：

- 自摸：`HandConfig(is_tsumo=True)`
- 立直：`HandConfig(is_riichi=True)`
- 一发：`HandConfig(is_ippatsu=True)`
- 场风：`HandConfig(round_wind=...)`
- 自风：`HandConfig(player_wind=...)`
- 规则项：`OptionalRules(has_open_tanyao=..., has_aka_dora=...)`

宝牌：

- 宝牌指示牌传入 `dora_indicators`
- 里宝牌指示牌传入 `ura_dora_indicators`

## 8. 输入校验

### 8.1 牌数校验

- 计算点数时，闭合手牌、和牌牌和副露应构成合法和牌输入。
- 每种实体牌最多 4 张。
- 红五和普通五同属同一基础牌，总数最多 4 张。
- 有副露时，闭合手牌数量应与副露数量匹配：
  - 无副露：闭合手牌 14 张。
  - 1 组副露：闭合手牌 11 张。
  - 2 组副露：闭合手牌 8 张。
  - 3 组副露：闭合手牌 5 张。
  - 4 组副露：闭合手牌 2 张。
- 杠子虽然显示 4 张，但按一个面子计算；传给 `mahjong` 库时仍需要包含杠子对应的 tiles。

### 8.2 副露校验

- 吃只能由同花色连续数牌组成，不能包含字牌。
- 碰必须三张相同基础牌。
- 杠必须四张相同基础牌。
- 暗杠不能让手牌变成“可立直”的副露状态；暗杠本身不破坏门前清。
- 明杠、碰、吃、加杠会破坏门前清。

### 8.3 场况校验

- 有开放副露时禁止立直、双立直、一发。
- 里宝牌在未立直时不参与计算，可保留输入但给出提示。
- 天和仅庄家自摸第一巡可成立，地和仅子家自摸第一巡可成立；前端至少应防止明显互斥状态。
- 海底和河底互斥。
- 立直和双立直互斥。

## 9. 移动端体验要求

- 所有主要按钮触控区域不小于 44px。
- 牌图在手机上应足够大，能准确点击。
- 选牌区固定在屏幕下方或易触达区域。
- 已选手牌区应横向滚动或自动换行，不能遮挡计算按钮。
- 场况信息默认收起，只显示关键摘要；点击后展开。
- 计算按钮固定在底部或结果区附近，状态明确。
- 输入错误应即时提示，避免用户到最后才发现。
- 常用开关应靠近计算按钮，例如自摸/荣和、是否庄家。

## 10. 推荐录入流程

1. 默认进入“手牌”录入模式。
2. 用户点选闭合手牌。
3. 用户长按或点击某张已选牌设置和牌牌。
4. 如有副露，点击“副露”，选择类型，再点牌快速生成。
5. 用户设置自摸/荣和、是否庄家、场风、自风。
6. 用户点选宝牌指示牌。
7. 用户点击“计算”。
8. 系统展示得点结果。

## 11. 验收标准

### 11.1 功能验收

- 可以在手机浏览器中完成完整录入并计算。
- 可以从 `img/` 的牌图中快速选择全部牌种。
- 可以录入并区分明杠和暗杠。
- 可以录入宝牌和里宝牌。
- 可以配置庄家、自风、场风、本场数等场况。
- 可以调用 `mahjong` 库返回点数、番数、符数、役种和符明细。
- 非法输入会阻止计算或返回明确错误。

### 11.2 体验验收

- 常见无副露手牌应能在 20 秒内完成录入并计算。
- 常见有 1 组副露手牌应能在 25 秒内完成录入并计算。
- 用户误点后能在 1 次点击内撤销或删除。
- 手机竖屏下不需要横向滚动整个页面。

### 11.3 示例验收

使用文档示例：

- 手牌：`22444m333567p444s`
- 和牌牌：`4s`
- 荣和时，应返回断幺九相关得点结果。
- 自摸并无副露时，应返回门前清自摸、断幺九、三暗刻等结果。
- 添加 `444m` 碰副露并启用食断时，应能返回开放断幺九结果。

## 12. 后续可扩展项

- 向听数计算。
- 和牌判定。
- 多规则配置预设。
- 本地历史记录。
- 分享计算结果。
- 牌姿截图生成。
- OCR 识别手牌。
